'''
Étape 3 : Pipeline 3 – Supplémentaire
Nouveau pipeline → Pipeline_Supplementaire.
À ce stade, tu peux mettre :
Test de preprocessing
Déploiement
Validation sur dataset réduit
Exemple simple'''

pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/Dreipfelt/Electricity-predict-Jenkins.git', branch: 'main'
            }
        }
        stage('Run supplementary tasks') {
            steps {
                sh 'python supplementary.py'
            }
        }
    }
}
